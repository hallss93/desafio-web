/**
 * @description             List of Types for use in Operations
 */
const FETCH_REPO = 'FETCH_REPO';
const FETCH_ONE_REPO = 'FETCH_ONE_REPO';
const SET_PAGE = 'SET_PAGE';
export default {
    FETCH_REPO,
    FETCH_ONE_REPO,
    SET_PAGE,
}